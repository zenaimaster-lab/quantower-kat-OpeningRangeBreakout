# Session: 2026-05-19

## Actions Taken
- Adjusted text positioning for 2m, 5m, and 15m lines in `OrderManager.mqh` `DrawORBLines` function.
  - 2m text anchor moved to left end (using `ANCHOR_RIGHT...` to extend right from `cTime`).
  - 5m text anchor moved to right end (using `ANCHOR_LEFT...` to extend left from `endTime`).
  - 15m text anchors changed to `ANCHOR_LOWER` (above) and `ANCHOR_UPPER` (below) to avoid strikethrough from the horizontal lines.
- Bumped `EA_VERSION` to "1.42" and updated `EA_BUILD_DATE` to "19 May 2026".

## Outcome
All UI elements render clearly without overlaps.

## Next Steps
None at the moment.
